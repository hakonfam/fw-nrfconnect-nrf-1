#include <autoconf.h>

spm:
  size: CONFIG_PM_PARTITION_SIZE_SPM
  placement:
    before: app
  inside: mcuboot_primary_app

  #if 0
spm_ram:
  size: CONFIG_PM_PARTITION_SIZE_SPM_RAM
  placement: {after: start}
  inside: ram_span_secure
  #endif
